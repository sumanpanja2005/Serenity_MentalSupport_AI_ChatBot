# This file lists the Python packages required to run the Mental Health Companion Chatbot.

# Flask is used for the web server and handling HTTP requests.
Flask>=2.0.0

# The official Google Gemini SDK for Python.
google-genai

# Pydantic is a dependency of google-genai and used internally for data validation.
# Specifying it ensures compatibility, though it is usually handled by the SDK package.
pydantic>=2.0.0